## ADDED Requirements

### Requirement: Delete Notebook Tool
The system SHALL provide an MCP tool `delete_notebook` to delete an existing notebook.

#### Scenario: User deletes a notebook
- **WHEN** the `delete_notebook` tool is invoked with a `notebook_id`
- **THEN** the system deletes the notebook
