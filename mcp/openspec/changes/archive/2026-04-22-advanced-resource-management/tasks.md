## 1. Resource Deletion Tools

- [x] 1.1 Implement and register `delete_notebook` tool in `tools.py`
- [x] 1.2 Implement and register `delete_source` tool in `tools.py`

## 2. Source Addition and Retrieval Tools

- [x] 2.1 Implement and register `add_youtube_source` tool in `tools.py`
- [x] 2.2 Implement and register `add_text_source` tool in `tools.py`
- [x] 2.3 Implement and register `get_source_fulltext` tool in `tools.py`

## 3. Chat Interaction Enhancements

- [x] 3.1 Update `ask_notebook` in `tools.py` to accept optional `source_ids` and `conversation_id` parameters, passing them to the client
- [x] 3.2 Implement and register `get_chat_history` tool in `tools.py`
- [x] 3.3 Implement and register `save_chat_note` tool in `tools.py`

## 4. Verification

- [x] 4.1 Run `fastmcp list src/nblm_mcp_server/server.py` to verify all new tools are registered and schemas are correct
